char* cur;

void strstart(char* tgt,int n)
{
	if (cur)
	{
		fprintf(stderr,"
		exit(-1);
	}
	cur = tgt;
}

void stradd(char* tgt,const char* a)
{
	int i = 0;
	if (a) while  (*a && i!=n) { *tgt++ = *a++; i++; }
	if (b) while  (*b && i!=n) { *tgt++ = *b++; i++; }
	return i;
}
